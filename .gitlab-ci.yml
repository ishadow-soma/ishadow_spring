deploy-to-server:
    stage: deploy
    only:
        - master
    script:
        - echo 'build start'
        - sudo su
        - cd /home/ubuntu/ishadow_backend_spring_latest
        - ./deploy.sh
        - ehco 'build end'
    tags:
        - deploy
