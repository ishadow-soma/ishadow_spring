deploy-to-server:
    stage: deploy
    only:
        - master
    script:
        - echo 'build start'
        - cd /home/ubuntu/ishadow_backend_spring_latest
        - sudo ./deploy.sh
        - ehco 'build end'
    tags:
        - deploy
