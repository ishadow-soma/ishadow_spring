package com.soma.ishadow.repository.user_review;

import com.soma.ishadow.domains.user_review.UserReview;

public interface UserReviewRepositoryQuerydsl {

    UserReview save(UserReview userReview);

}
